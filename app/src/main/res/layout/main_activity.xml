<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View"/>
        <import type="com.kapi.ca.ui.main.MainViewModel.MainFragment"/>
        <variable
            name="viewModel"
            type="com.kapi.ca.ui.main.MainViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="#FFE277"
        android:clipChildren="false">

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/fragmentContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:name="com.kapi.ca.ui.home.HomeFragment"/>

        <androidx.appcompat.widget.LinearLayoutCompat
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintDimensionRatio="360:70"
            android:orientation="horizontal">

            <androidx.appcompat.widget.AppCompatImageView
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:src="@{viewModel.selectedFragment == MainFragment.ANIMATION ? @drawable/ic_animation_selected : @drawable/ic_animation}"
                android:onClick="@{(_) -> viewModel.onAnimationClick()}"/>

            <androidx.appcompat.widget.AppCompatImageView
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:src="@{viewModel.selectedFragment == MainFragment.HOME ? @drawable/ic_home_selected : @drawable/ic_home}"
                android:onClick="@{(_) -> viewModel.onHomeClick()}"/>

            <androidx.appcompat.widget.AppCompatImageView
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:src="@{viewModel.selectedFragment == MainFragment.SETTINGS ? @drawable/ic_options_selected : @drawable/ic_options}"
                android:onClick="@{(_) -> viewModel.onSettingsClick()}"/>

        </androidx.appcompat.widget.LinearLayoutCompat>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>